class Spell
  def initialize(options)
    id = options['id'].to_i unless options['id'] == nil
    name = options['name']
    c_class = options['class']

  end
end

class Learning
  def initialize()
    id = options['id'].to_i unless options['id'] == nil
  end
end

class Spellbook
  def initialize()
    id = options['id'].to_i unless options['id'] == nil
  end
end
